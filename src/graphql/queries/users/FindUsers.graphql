query findUsers($FindUsersInput: FindUsersInput!) {
  findUsers(query: $FindUsersInput) {
    code
    success
    totalCount
    page
    lastPage
    hasMore
    paginatedUsers {
      ...user
    }
  }
}
