query getAllPosts($page: Float!, $limitPerPage: Float!) {
  getAllPosts(page: $page, limitPerPage: $limitPerPage) {
    code
    success
    hasMore
    lastPage
    totalCount
    paginatedPosts {
      id
      photo
      caption
      location
    }
  }
}
